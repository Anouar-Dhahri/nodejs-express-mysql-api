const express = require('express');
const router = express.router();
const { getUsers, createUser, findUser, deleteUser } = require('../controllers/UserController');

router.get('/get',);

router.get('/find',);

router.post('create',);

router.put('/update',);

router.delete('/delete',);

module.exports = router;
